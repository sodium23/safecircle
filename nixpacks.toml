[phases.setup]
providers = ["python"]

[phases.install]
cmds = ["python -m venv /app/.venv", "/app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
